#### Install dependencies
` composer install `

#### Run migrations
` php artisan migrate `

#### Install passport client
` php artisan passport:install `

#### Run DB Seeders
` php artisan db:seed `

#### Run application
` php artisan serve `
